name: Behaviour Load Test (Locust)

on:
  workflow_dispatch:
    inputs:
      profile:
        description: "Load profile from tests/locust/load_profiles"
        required: true
        default: "light_mixed"
        type: string

      base_url:
        description: "Base application URL (ePrihlasky frontend / API)"
        required: false
        default: "https://test-eprihlasky.iedu.sk"
        type: string

      idp_url:
        description: "Base TIAM Identity Provider URL"
        required: false
        default: "https://tiamidsk.iedu.sk"
        type: string

      username:
        description: "Login username (overrides GitHub secret if provided)"
        required: false
        type: string

      password:
        description: "Login password (overrides GitHub secret if provided)"
        required: false
        type: string

permissions:
  contents: write   # 👈 POTREBNÉ na push do github-pages

jobs:
  behaviour:
    uses: ./.github/workflows/base_runtime.yml
    secrets: inherit

    with:
      base_url: ${{ inputs.base_url }}
      idp_url:  ${{ inputs.idp_url }}
      username: ${{ inputs.username }}
      password: ${{ inputs.password }}

      extra_env: |
        export LOAD_PROFILE_NAME=${{ inputs.profile }}

      python_command: |
        set +e

        echo "▶ Starting Locust with profile: ${LOAD_PROFILE_NAME}"

        python3 -m locust \
          -f tests/locust/full_flow_spawn.py \
          --headless \
          --csv tests/locust/temp/results \
          --html tests/locust/temp/report.html \
          --only-summary

        LOCUST_EXIT_CODE=$?

        python3 tests/locust/utils/generate_pdf_report.py
        python3 tests/locust/utils/generate_loadtest_dashboard.py

        exit ${LOCUST_EXIT_CODE}

  publish:
    name: Publish to GitHub Pages
    runs-on: ubuntu-latest
    needs: behaviour
    if: always()

    steps:
      - name: Checkout github-pages branch
        uses: actions/checkout@v4
        with:
          ref: github-pages

      - name: Clean old loadtest reports
        run: |
          rm -rf loadtest
          mkdir -p loadtest

      - name: Copy new loadtest reports
        run: |
          cp -r pages/loadtest/* loadtest/

      - name: Commit and push reports
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          git add loadtest
          git commit -m "📊 Load test report (${GITHUB_RUN_NUMBER})" || echo "Nothing to commit"
          git push
