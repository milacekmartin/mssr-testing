name: Prihlaska Tests

on:
  workflow_dispatch:
    inputs:
      username:
        description: "Login username (optional – overrides secret)"
        required: false
      password:
        description: "Login password (optional – overrides secret)"
        required: false
  push:
    branches: [ main, master ]

jobs:
  prihlaska-tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install requests beautifulsoup4

      - name: Resolve credentials
        run: |
          USERNAME="${{ github.event.inputs.username || secrets.LOGIN_USERNAME }}"
          PASSWORD="${{ github.event.inputs.password || secrets.LOGIN_PASSWORD }}"

          if [ -z "$USERNAME" ] || [ -z "$PASSWORD" ]; then
            echo "❌ Missing credentials"
            exit 1
          fi

          echo "LOGIN_USERNAME=$USERNAME" >> $GITHUB_ENV
          echo "LOGIN_PASSWORD=$PASSWORD" >> $GITHUB_ENV

      - name: Run full prihlaska flow
        run: |
          echo "🚀 Running PRIHLASKA full flow..."
          python3 tests/prihlaska/full_flow.py --show-data

      - name: Upload outputs
        uses: actions/upload-artifact@v4
        with:
          name: prihlaska-tests-output
          path: |
            **/*.log
            **/*.json
            **/*.txt
